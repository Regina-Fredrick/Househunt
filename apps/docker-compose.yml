services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: househunt
      POSTGRES_USER: househunt_user
      POSTGRES_PASSWORD: changeme
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: